from utils.parser import parse_inputs
from utils.calendar import generate_clinic_calendar

# Step 1: Parse all inputs (YAML, CSVs)

# Step 2: Build calendar

# Step 3: Build model using calendar + rules

# Step 4: Solve and extract schedule